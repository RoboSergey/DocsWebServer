services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - doc_data:/data
    environment:
      - DATABASE_PATH=/data/documents.db
      - HOST_IP=192.168.1.100  # <-- replace with your machine's LAN IP
    restart: unless-stopped

volumes:
  doc_data:
