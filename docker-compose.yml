services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - doc_data:/data
    environment:
      - DATABASE_PATH=/data/documents.db
      - HOST_IP=192.168.0.126
    restart: unless-stopped

volumes:
  doc_data:
